@AGENTS.md

## Project spec
The authoritative build spec is docs/SPEC.md — data model, scheduler contract,
importer contract, and the numbered build order. Follow it; treat its "Resolved
Decisions" as fixed. Prefer `npx expo install` over `npm install`.